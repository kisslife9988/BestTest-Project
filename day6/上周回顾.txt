上周回顾：
    函数：
        def my(xx,xx=None):
            return   结束函数、返回函数结果、
            返回多个值的时候，接收也得用三个变量；如果用一个变量接收则生成一个元组
            非空即真、非零即真
            局部变量、全局变量
            函数中修改全局变量使用global、字典和list是不用声明直接可以修改
            递归：自己调用自己的函数
    集合：d = {} 字典
          d = set（）集合
          集合天生去重
    模块：
        标准模块--Python自带
        第三方模块
        自己开发的模块